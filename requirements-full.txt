# ============================================================
# 完整依赖（512MB / 1GB 档位，含可视化图表库，约 +120MB）
# ============================================================
-r requirements.txt

# 数据可视化（generate_report_image 功能需要）
matplotlib>=3.7.0
seaborn>=0.12.0
